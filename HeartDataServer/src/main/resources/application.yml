server:
   port: 80
   context-path: /
mybatis:
   configuration:
# 开启驼峰uName自动映射到u_name
      map-underscore-to-camel-case: true
spring:
# mysql
   datasource:
      url: jdbc:mysql://localhost:3306/heartdata
      username: root
      # mac / windows passport
      password: root
      driver-class-name: com.mysql.jdbc.Driver
fileBasePath: ./heartFils/